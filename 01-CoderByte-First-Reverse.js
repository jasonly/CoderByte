//First Reverse
function firstReverse(str) {
	return str.split('').reverse().join('')
};

//split('') breaks up the str argument into individual characters
//reverse() reverses the string
//join('') joins these reversed characters